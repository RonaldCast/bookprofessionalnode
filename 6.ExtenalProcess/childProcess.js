'use strict'

/*const child_process = require('child_process');


var optional = {
    timeout: 1,
    killSignal: 'SIGKILL' //valos que enviara si el la se;an el 
                        //tiempo o maxBuffer hacido superado
}


//ejecutar comandos externos en las shell exec(command, callback)

var exec = child_process.exec;
exec('cd he', optional, (err, stdout, stderr) => {
    setTimeout(() =>{
         console.log("hellos", stdout)
    }, 1000)
   
})*/

//copiando la varaible de entorno 
